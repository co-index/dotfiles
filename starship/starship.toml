# Starship defaults, except: no blank line between prompts, the prompt
# character stays on the same line, and no Nerd Font branch icon (the
# terminal font here cannot render it).
add_newline = false

[line_break]
disabled = true

[git_branch]
symbol = ""
